# Service Registry

The Service Registry describes the services available to dm.

It provides the discovery surface that powers `dm service list` and
`dm service describe`. Runtime invocation will be added separately.
