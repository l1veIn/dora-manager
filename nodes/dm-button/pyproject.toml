[project]
name = "dm-button"
version = "0.1.0"
description = "A trigger button widget for the Interaction UI."
requires-python = ">=3.10"
dependencies = ["dora-rs == 0.4.1", "pyarrow"]

[project.scripts]
dm-button = "dm_button.main:main"
