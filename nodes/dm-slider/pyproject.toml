[project]
name = "dm-slider"
version = "0.1.0"
description = "A slider widget for the Interaction UI."
requires-python = ">=3.10"
dependencies = ["dora-rs == 0.4.1", "pyarrow", "requests", "websocket-client"]

[project.scripts]
dm-slider = "dm_slider.main:main"
