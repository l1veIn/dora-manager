# dm-display

`dm-display` is an interaction-family node.

- `path` input is for stored artifacts such as images, audio, video, and other files under `runs/:id/out/`
- `data` input is for lightweight inline content such as text, markdown, and JSON that should be rendered directly without going through artifact storage
