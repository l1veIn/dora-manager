# dm-text-reserve

`dm-text-reserve` is a tiny text-transform node for demos.

- It accepts one `text` input.
- It emits one `text` output.
- Current behavior: reverse the incoming string so interaction demos show a visible transformation step between input and message sink.
