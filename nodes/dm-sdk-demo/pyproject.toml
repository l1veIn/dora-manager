[project]
name = "dm-sdk-demo"
version = "0.1.0"
description = "SDK interaction demo — no bridge needed"
requires-python = ">=3.10"
dependencies = ["dora-rs == 0.4.1", "pyarrow"]

[project.scripts]
dm-sdk-demo = "dm_sdk_demo.main:main"
