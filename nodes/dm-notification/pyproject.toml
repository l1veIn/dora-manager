[project]
name = "dm-notification"
version = "0.1.0"
description = "System notification utility node for Dora Manager."
requires-python = ">=3.10"
dependencies = ["dora-rs == 0.4.1", "pyarrow", "requests"]

[project.scripts]
dm-notification = "dm_notification.main:main"
