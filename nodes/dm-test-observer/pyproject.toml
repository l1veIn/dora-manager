[project]
name = "dm-test-observer"
version = "0.1.0"
description = "Builtin DM test node for aggregating multimodal test events"
requires-python = ">=3.10"
dependencies = ["dora-rs >= 0.3.9", "pyarrow"]

[project.scripts]
dm-test-observer = "dm_test_observer.main:main"
