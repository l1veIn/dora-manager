[project]
name = "dm-message"
version = "0.1.0"
description = "Emit a human-visible run message into the DM interaction plane"
requires-python = ">=3.10"
dependencies = ["dora-rs == 0.4.1", "pyarrow"]

[project.scripts]
dm-message = "dm_message.main:main"
