[project]
name = "dm-input"
version = "0.1.0"
description = "Register web input widgets with dm-server and emit user actions back into the dataflow"
requires-python = ">=3.10"
dependencies = ["dora-rs >= 0.3.9", "pyarrow", "requests"]

[project.scripts]
dm-input = "dm_input.main:main"
