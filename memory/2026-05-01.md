# 2026-05-01

## 完成

### SDK Interaction 全链路验证通过
- testtest dataflow (1 node: dm-sdk-demo) 端到端跑通
- **验证流程：** Web UI 输入文字 → dm-server 存储 → Python SDK get() 拉取 → 反转字符串 → SDK send() 写回 → Web UI 显示
- 结果：`Hello from Synnia!` → `**Reversed:** !ainnyS morf olleH`

### 修复
- `~/Desktop/dora-manager/nodes/dm-sdk-demo/dm_sdk_demo/main.py` subscriber_loop()
- **Bug：** 对 `payload.to` 的过滤条件写的是 `"input-text"`（旧 widget 注册的 node_id），但 Web UI 发送的 `to` 是 `"dm-sdk-demo"`（dora 节点名），导致消息被跳过
- **Fix：** 去掉 target 过滤，demo 只有单个节点不需要过滤
- 另外清理了一个从周三开始运行、CPU 200% 的残留 dm-sdk-demo 进程

### WebSocket subscribe 改造
- 将 HTTP 轮询 `msg.get(tag="input")` 改为 WebSocket `msg.subscribe(tag="input")`
- 代码从 ~20 行的 poll 循环（带锁、seq 追踪、sleep）降到 ~10 行
- 延迟从最坏 0.5s 降到实时推送

### FaaS 全链路集成 (SDK → dm-server FaaS → Web UI)
- FaaS (faas-demo function) 已嵌入 dm-server（不再有单独的 dm-faasd 二进制）
- 重写了 faas-demo function：输入 text 返回 original / reversed / length
- SDK demo node 收到用户输入后，调 `POST /api/fn/faas-demo/invoke` 反转，结果 send 到 Web UI
- 全链路验证：`"FaaS is running inside dm-server!"` → `"!revres-md edisni gninnur si SaaF"`

## 待办
- [ ] dm-faasd Phase 3 构建验证（已无独立二进制，FaaS 嵌入 dm-server）
- [ ] YOLO demo
